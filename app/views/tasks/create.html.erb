<h3><span>【<%= current_user.last_name %>さん】個人の仕事</span>にその他の仕事の予定を追加</h3>

<div>
  <span>仕事の件名</span><%= @task.title %>
</div>
<div>
  <span>仕事の内容</span><%= @task.introduction %>
</div>
<div>
  <span>ステータス</span><%= @task.deadline_status %>
  <% if @task.deadline_status == "締切あり" %>
    <%= @event.end_date %>締切
  <% end %>
</div>
<div>
  <span>参加者</span>
  <% if @event.team_id.present? %>
    <% team = Team.find(@event.team_id) %>
    <span>【<%= team.team_name %>】チームの仕事</span>
  <% else %>
    <span>【<%= current_user.last_name %>さん】個人の仕事（自分のみ）</span>
  <% end %>
</div>

<%= form_with model: @event, url: "/tasks/#{@task.id}", local: true do |f| %>
  <%= f.hidden_field :task_id, value: @task.id %>

  <% unless @event.end_date.present? %>
    <%= f.hidden_field :end_date, value: @event.start_date.since(3.years) %>
  <% end %>

  <%= f.submit '上記内容の仕事を追加' %>
<% end %>

<%= link_to "仕事の追加をキャンセル", "/tasks/#{@task.id}", method: :delete %>